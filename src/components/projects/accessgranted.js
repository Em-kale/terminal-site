import React from "react"
import CustomWindow from "../common/customwindow"
const AccessGranted = (props) => {
   
    const {setFinished, type, zIndex} = props 

    return ( 
    <>
        <CustomWindow />
    </>
        
    )
}
export default AccessGranted